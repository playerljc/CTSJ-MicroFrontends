import React from 'react';
import type { IRegisterConfig } from './types';
/**
 * createComponent - 创建路由组件
 * @param config
 * @param container
 * @return React.ReactElement
 */
export declare function createComponent(config: IRegisterConfig, container: HTMLElement): React.ReactElement;
